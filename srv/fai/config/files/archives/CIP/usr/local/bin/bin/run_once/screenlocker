#!/bin/bash

# timeout to blank screen in seconds
gsettings set org.gnome.desktop.session idle-delay 180

# timeout to lock screen after screen is blank 
gsettings set org.gnome.desktop.screensaver lock-delay 240

