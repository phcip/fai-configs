#!/bin/bash

# Check if monitor disconnected and reconnect it (XFCE power manager bug)

while true; do
	if ! xrandr | grep -E '(DP1|DP-1) connected 3840x2160+0+0 (normal left inverted right x axis y axis) 530mm x 300mm' &> /dev/null; then
		xrandr --auto
		sleep 5
	fi
done

exit 0
